{
  "name": "primer-api",
  "workspaces": [
    "api",
    "examples/docs"
  ],
  "scripts": {
    "setup": "./bin/setup.sh",
    "dev": "concurrently -n api,docs \"npm run dev --w api\" \"npm run dev --w docs\""
  },
  "devDependencies": {
    "concurrently": "^7.3.0"
  }
}
